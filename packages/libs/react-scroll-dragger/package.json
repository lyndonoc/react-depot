{
  "name": "react-scroll-dragger",
  "version": "1.0.14",
  "author": "Lyndon Ohhyung Chun <lyndonoc@gmail.com>",
  "main": "dist/index.js",
  "dependencies": {
    "@emotion/core": "^10.0.28",
    "@emotion/styled": "^10.0.27",
    "classnames": "^2.2.6",
    "prop-types": "^15.7.2",
    "react": "^16.12.0",
    "react-custom-scrollbars": "^4.2.1",
    "react-dom": "^16.12.0",
    "react-syntax-highlighter": "^12.2.1",
    "smoothscroll-polyfill": "^0.4.4"
  },
  "scripts": {
    "watch": "npm run compile:watch",
    "compile": "babel src -d dist --copy-files --no-copy-ignored --delete-dir-on-start --root-mode upward",
    "compile:watch": "npm run compile -- --watch --verbose",
    "prepublish": "NODE_ENV=production npm run compile",
    "test": "react-scripts test"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
